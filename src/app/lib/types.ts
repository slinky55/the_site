type User = {
    id: string;
    name: string;
    email: string;
    emailVerified: boolean;
    image: string;
    passwordHash: string;
    privilegeLevel: string;
    createdAt: string;
    updatedAt: string;
}